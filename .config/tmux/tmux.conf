# Globals --------------------------------------------------------------------
set -g prefix C-s
set -g mouse on
set-option -g status-position bottom #top

unbind r
bind r source-file ~/.config/tmux/tmux.conf

# Start windows and panes index at 1, not 0.
set -g base-index 1
setw -g pane-base-index 1

# Ensure window index numbers get reordered on delete.
set-option -g renumber-windows on

# :checkhealth nvim ----------------------------------------------------------

set-option -g focus-events on
set-option -g default-terminal "screen-256color"
set-option -a terminal-features 'xterm-256color:RGB'


# Binds ----------------------------------------------------------------------

bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key [ split-window
bind-key ] split-window -h

bind-key H swap-window -t -1\; select-window -t -1
bind-key L swap-window -t +1\; select-window -t +1

#C-s {          move the current pane to the previous position
#C-s }          move the current pane to the next position
#C-s C-o        rotate window ‘up’ (i.e. move all panes)
#C-s M-o        rotate window ‘down’

# Status Line ----------------------------------------------------------------

set -g status-right-length 100
set -g status-left-length 100
set -g status-left ""
set -g status-right '#[fg=#{@thm_crust},bg=#{@thm_teal}] #S '

# Theme ----------------------------------------------------------------------

set -g @catppuccin_flavor 'mocha' # mocha, latte, machiatto
set -g @catppuccin_window_status_style "basic"
#set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_text " #W"
set -g @catppuccin_window_text " #W"
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

set -g status-right '#[fg=#{@thm_crust},bg=#{@thm_teal}] #S #[default] #[fg=white] %H:%M ' # [fg=black|white]

# Sessions -------------------------------------------------------------------

# TCC session
new-session -d -s TCC -n opencode -c /home/thr/Faculdade/TCC/ibr-etl 'source venv/bin/activate && opencode'
new-window -t TCC -n ibr-etl -c /home/thr/Faculdade/TCC/ibr-etl 'source venv/bin/activate'
new-window -t TCC -n nvim -c /home/thr/Faculdade/TCC/ibr-etl 'source venv/bin/activate && nvim'
new-window -t TCC -n datasets -c /home/thr/Faculdade/TCC/ibr-etl/datasets
new-window -t TCC -n RUNNING -c /home/thr/Faculdade/TCC/ibr-etl

# Homelab session
new-session -d -s server -c /home/thr/Arr_Stack/github-repo

